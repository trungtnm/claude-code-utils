{
  "description": "Warn at session start when git-tracked beads JSONL has drifted ahead of the local DB (mtime fooled by a git checkout/pull), so beads auto-import does not silently clobber local issues.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/beads-guard.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
