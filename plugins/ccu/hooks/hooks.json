{
  "description": "SessionStart guard: warn when git-tracked beads JSONL has drifted ahead of the local DB, so auto-import does not silently clobber local issues.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}/hooks/beads-guard.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
